# `class_methods do` is the block form of `module ClassMethods`, and Concern
# creates the same module either way. Session 13 recorded the methods without
# the module and pinned that as deliberate; session 26 measured what it costs
# and reversed it. On discourse, `Service::Base` writes
# `class_methods do include StepsHelpers end`, and the methods that reaches —
# `step`, `model`, `policy` — are the largest single bucket of declined app
# sites in the corpus.
#
# The second assertion is the one that pays: a mixin inside the block is a
# *class-side* ancestor of every includer, not an instance-side one of the
# concern.
symbols app.rb Auditable,ClassMethods,audit_log,record,Reporting,report,Widget
def app.rb:24:3 status=resolved owner=Auditable::ClassMethods site=app.rb:7
def app.rb:25:3 status=resolved owner=Reporting site=app.rb:17
